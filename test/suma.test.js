const suma = require('../src/suma');

describe('Pruebas para la función suma', () => {
    test('sumar 1 + 2 es igual a 3', () => {
        expect(suma(1, 2)).toBe(3);
    });

    test('sumar 1 + 2 es igual a 3', () => {
        expect(suma(1, 2)).toBe(3);
    });
})

describe('Pruebas para la función suma 2', () => {
    test('sumar 1 + 2 es igual a 3', () => {
        expect(suma(1, 2)).toBe(3);
    });

    test('sumar 1 + 2 es igual a 3', () => {
        expect(suma(1, 2)).toBe(3);
    });
})